# Padosi Politics - Frontend Development Environment
# For local development with Vite proxy

# API URL - Leave empty or use /api for Vite proxy (recommended)
# The Vite dev server proxies /api requests to localhost:5000
VITE_API_URL=
